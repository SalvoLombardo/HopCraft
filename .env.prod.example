# ==============================================================================
# HopCraft — Variabili d'ambiente PRODUZIONE
# Copia in /opt/hopcraft/.env.prod sull'EC2 e compila i valori reali.
# Questo file viene caricato da docker-compose.prod.yml (env_file: .env.prod)
# NON committare .env.prod — è nel .gitignore
# ==============================================================================

# --- Database ------------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://hopcraft:CAMBIA_ME@postgres:5432/hopcraft
DB_PASSWORD=CAMBIA_ME                   # usato anche da postgres: nel compose

# --- Redis ---------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# --- Flight Provider -----------------------------------------------------------
FLIGHT_PROVIDER=amadeus
AMADEUS_API_KEY=CAMBIA_ME
AMADEUS_API_SECRET=CAMBIA_ME

# --- LLM Provider --------------------------------------------------------------
LLM_PROVIDER=gemini
GEMINI_API_KEY=CAMBIA_ME
GROQ_API_KEY=CAMBIA_ME
MISTRAL_API_KEY=CAMBIA_ME

# --- App -----------------------------------------------------------------------
APP_ENV=production
CACHE_TTL_HOURS=6
MAX_AIRPORTS_SEARCH=300
